{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/zden.schema.json",
  "title": "Zden's Visual Crypto Puzzles",
  "description": "Artistic puzzles where private keys are encoded in images, animations, and visual patterns across multiple blockchains",
  "type": "object",
  "properties": {
    "author": {
      "$ref": "./definitions.schema.json#/$defs/author"
    },
    "metadata": {
      "$ref": "./definitions.schema.json#/$defs/metadata"
    },
    "puzzles": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/puzzle"
      },
      "description": "Array of visual puzzles across multiple chains"
    }
  },
  "required": ["puzzles"],
  "additionalProperties": true,
  "$defs": {
    "puzzle": {
      "type": "object",
      "description": "Single Zden visual puzzle",
      "properties": {
        "name": {
          "type": "string",
          "description": "Puzzle name (e.g., Level 1, Level 5, Level HALV)"
        },
        "chain": {
          "$ref": "./definitions.schema.json#/$defs/chain"
        },
        "address": {
          "$ref": "./definitions.schema.json#/$defs/address"
        },
        "status": {
          "$ref": "./definitions.schema.json#/$defs/status"
        },
        "prize": {
          "type": ["number", "null"],
          "description": "Prize amount in native currency"
        },
        "pubkey": {
          "$ref": "./definitions.schema.json#/$defs/pubkey"
        },
        "key": {
          "type": ["object", "null"],
          "description": "Private key information (optional for unsolved puzzles)",
          "properties": {
            "hex": {
              "type": ["string", "null"],
              "pattern": "^[a-f0-9]{64}$"
            },
            "wif": {
              "$ref": "./definitions.schema.json#/$defs/wif"
            },
            "seed": {
              "$ref": "./definitions.schema.json#/$defs/seed"
            },
            "mini": {
              "type": ["string", "null"]
            },
            "bits": {
              "type": ["integer", "null"],
              "minimum": 1,
              "maximum": 256
            },
            "shares": {
              "$ref": "./definitions.schema.json#/$defs/shares"
            }
          },
          "additionalProperties": true
        },
        "start_date": {
          "type": ["string", "null"],
          "pattern": "^\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}$"
        },
        "solve_date": {
          "type": ["string", "null"],
          "pattern": "^\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}$"
        },
        "solve_time": {
          "type": ["integer", "null"],
          "description": "Time to solve in seconds"
        },
        "source_url": {
          "type": ["string", "null"]
        },
        "transactions": {
          "type": "array",
          "items": {
            "$ref": "./definitions.schema.json#/$defs/transaction"
          }
        },
        "solver": {
          "type": ["string", "null"],
          "description": "Solver ID reference"
        },
        "assets": {
          "$ref": "./definitions.schema.json#/$defs/assets"
        }
      },
      "required": ["name", "chain", "address", "status"],
      "additionalProperties": true
    }
  }
}
